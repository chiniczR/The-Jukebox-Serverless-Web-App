window._config = {
    cognito: {
        userPoolId: '', // Your user pool ID
        userPoolClientId: '', // Your user pool client ID
        region: '' // The region of your Cognito user pool
    },
    api: {
        invokeUrl: ''   // Invoke URL for you API Gateway (stage-specific)
    },
    identity: {
        idPoolId: '', // Your identity pool ID
        region: '' // The region of your Cognito identity pool
    }
};